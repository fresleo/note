v2ray:bash <(curl -s -L https://git.io/v2ray-setup.sh)

如何检查 BBR 是否已开启？
请在 Debian 12 终端中运行以下两条命令：

1. 检查当前的拥塞控制算法

sysctl net.ipv4.tcp_congestion_control
如果返回： net.ipv4.tcp_congestion_control = bbr，说明已经开启了。

如果返回： net.ipv4.tcp_congestion_control = cubic，说明尚未开启。


如果没开，如何在 Debian 12 上开启 BBR？
开启 BBR 非常简单，不需要安装任何额外软件，只需两步：

第一步：将 BBR 配置写入系统设置
直接复制并运行以下整段命令，它会将开启 BBR 所需的参数写入到系统配置文件中：


sudo bash -c 'cat > /etc/sysctl.d/99-bbr.conf << EOF
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr
EOF'
第二步：使配置立即生效
运行以下命令刷新系统配置，无需重启服务器：


sudo sysctl --system
执行完后，你可以再次运行前面的 sysctl net.ipv4.tcp_congestion_control 命令，只要看到输出变成 bbr，就说明你的 Debian 12 已经成功起飞，网络加速生效了！
